# Handoff: ядро конфигурации Task 7

- **Дата:** 2026-07-16
- **Ветка:** `agent/config-core`
- **Статус дерева:** изменения Task 7 подготовлены к отдельному commit

## Выполнено

- Добавлены defaults `GhostTermConfig`, typed parser `HotKeyDescriptor`, byte-preserving `ConfigDocument` и line diagnostics.
- Добавлены transactional `ConfigController`, atomic effective write и coalescing `ConfigFileWatcher` с узкими test seams.
- Добавлены starter config, русскоязычный справочник и 12 targeted tests.

## Проверки

- `swift format lint GhostTerm/Config/*.swift GhostTerm/Input/HotKeyDescriptor.swift GhostTermTests/Config/*.swift` — успешно.
- Временный Swift package с Xcode toolchain: 12 targeted tests — успешно.
- Полный Xcode target не запускался: submodule в worktree не инициализирован.

## Незавершённое

- Интеграция с `AppDelegate`, `GhosttyBridge`, diagnostic banner и menu commands отложена по условию задачи.
- Ресурсы не подключены в build phase, поскольку `project.yml` намеренно не изменялся.

## Следующий шаг

1. В интеграционной ветке добавить `GhostTerm/Resources/default-config` и `GhostTerm/Resources/configuration-reference.md` в resources target и создать `ConfigController.production` с closure `bridge.reloadConfig(at:)`.

## Важный контекст

- Watcher наблюдает только пользовательский `config`, поэтому запись `.ghostty-effective-config` не образует reload-loop.
- При ошибке Ghostty reload controller восстанавливает предыдущие active config и effective bytes.
