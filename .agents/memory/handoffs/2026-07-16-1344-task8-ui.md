# Handoff: Task8 tab bar и workspace selector

- **Дата:** 2026-07-16
- **Ветка:** `agent/ui-task8`
- **Статус дерева:** чистое после task commit

## Выполнено

- Добавлен компактный AppKit chrome: selector workspace, collection-view tab bar и terminal content container.
- `WindowCoordinator` создаёт `Default` с одним tab/pane, чей `PaneID` совпадает с реальной Ghostty surface; surface встраивается через `WorkspaceViewController`.
- Добавлены multi-selection, reorder foundation, close/broadcast UI и меню move; Move to New Workspace использует sheet с inline validation.
- Единственная runtime surface при перемещении переустанавливается в контейнер без перезапуска процесса.

## Проверки

- `make format` — успешно.
- `make lint` — успешно.
- Targeted `TabSelectionModelTests` и `WorkspacePresentationTests` — 8 тестов успешно.
- Debug app собран в targeted test action и запущен smoke-командой — процесс стартовал и штатно завершён.

## Незавершённое

- Новые runtime surfaces/tabs не создаются до Task9/runtime mapping.
- Duplicate into Workspace намеренно disabled.

## Следующий шаг

1. При интеграции Task9 использовать `WorkspaceViewController.displayTerminal(_:)` как временную single-pane границу и заменить её recursive split presentation без пересоздания surface.

## Важный контекст

- `WorkspaceStore` остаётся source of truth; пустой workspace безопасно показывает placeholder.
- Persistence, Config, Quake, Vendor pin/content и Task9 split files не изменялись.
