# Handoff: config watcher echo

- **Дата:** 2026-07-16
- **Ветка:** `agent/quake-tabs-polish`
- **Статус дерева:** изменения готовы к коммиту

## Выполнено

- `ConfigController.reload()` игнорирует документ, byte-for-byte равный `activeDocument`.
- Добавлен тест echo после `updateQuakeHeight`; внешний изменённый документ повторно применяется.

## Проверки

- `make format` — пройдено.
- `make lint` — пройдено.
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild ... -only-testing:GhostTermTests/ConfigControllerTests -only-testing:GhostTermTests/ConfigDocumentTests` — 16 тестов пройдены.

## Незавершённое

- Нет.

## Следующий шаг

1. Создать коммит `fix: ignore echoed config writes`.

## Важный контекст

- Guard находится после чтения и создания `ConfigDocument`, поэтому касается только идентичных валидно применённых documents; изменённая невалидная конфигурация продолжает выдавать diagnostics.
