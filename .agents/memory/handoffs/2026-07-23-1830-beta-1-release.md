# Handoff: публичный QuickTTY 0.1.0-beta.1

- **Дата:** 2026-07-23
- **Ветка:** `master`
- **Статус дерева:** чистое после commit/push release evidence

## Выполнено

- Опубликован GitHub prerelease: `https://github.com/dntsk/quicktty/releases/tag/v0.1.0-beta.1`.
- Lightweight tag `v0.1.0-beta.1` указывает на release commit `10f97964da5a0d573f53b61faf335b35fab8142e`.
- Bundle metadata: `CFBundleShortVersionString = 0.1.0`, `CFBundleVersion = 3`.
- Artifact: `QuickTTY-0.1.0-beta.1-arm64.dmg`, `9138481` bytes.
- SHA-256: `be4e85c1eb76ba4e557f5e09813ec14f3510569d7979b230a4a534a65323e463`.
- Apple submission ID: `15f38a78-170d-4bd1-8433-147f9d10b944`, status `Accepted`.
- Для notarization явно использован существующий Keychain profile `ghostterm-notary`; credentials и secret values не читались и не выводились.
- Добавлен нейтральный `make signed-release`; `make signed-alpha` сохранён как совместимый alias.

## Проверки

- `make release-contract` — PASS.
- `make notarize-contract` — PASS.
- Integrated release diff review — APPROVED, Critical/Important нет.
- `.agents/scripts/pre-deploy-check.sh` — PASS; `make check`: 569 тестов, 27 suites, 0 failures.
- Release archive — arm64 only, build 3, hardened runtime и строгая Developer ID подпись проверены.
- `stapler validate`, повторный `codesign` и Gatekeeper — PASS; source `Notarized Developer ID`.
- GitHub asset: state `uploaded`, размер и digest совпадают с локальным DMG.
- Анонимное скачивание без GitHub credentials — PASS, SHA-256 совпал.

## Незавершённое

- Опубликованный DMG не устанавливался и не запускался; manual smoke не выполнялся по ограничению сессии.
- Профиль `quicktty-notary` не создавался; пока используется explicit `ghostterm-notary`.

## Следующий шаг

1. Только по явному разрешению пользователя установить опубликованный DMG и выполнить manual smoke.
2. После этого вернуться к дизайну agent-aware status integration.

## Важный контекст

- Не перемещать и не перезаписывать tag/assets `v0.1.0-beta.1`; исправления выпускать новой версией.
- Release tag намеренно остаётся на проверенном release commit, а project-memory evidence коммитится поверх него в `master`.
- Artifact и notarization evidence находятся в ignored `.build/Release/`.
