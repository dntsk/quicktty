# Handoff: иконка QuickTTY

- **Дата:** 2026-07-22
- **Ветка:** rename/quicktty
- **Статус дерева:** подготовлены изменения Task 4 для коммита `design: add QuickTTY application icon`

## Выполнено

- `scripts/generate-app-icon.swift` заменён утверждённым детерминированным рендером Q/TTY с CLI-путём `<output.appiconset>`.
- Обновлены десять PNG в `QuickTTY/Assets.xcassets/AppIcon.appiconset`.
- В генераторе нет временных путей, внешних шрифтов, сети, зависимостей или старой G/T-композиции.

## Проверки

- `swift format format --in-place scripts/generate-app-icon.swift` — PASS.
- `swift scripts/generate-app-icon.swift QuickTTY/Assets.xcassets/AppIcon.appiconset` — PASS.
- `sips -g format -g pixelWidth -g pixelHeight -g hasAlpha QuickTTY/Assets.xcassets/AppIcon.appiconset/*.png` — 10 RGBA PNG с ожидаемыми размерами и alpha.
- `cmp` для master 1024 — байт-в-байт совпадает с утверждённым `/tmp/quicktty-icon-preview-1024.png`.
- `swift scripts/generate-app-icon.swift /tmp/quicktty-icon-repro-check.appiconset` и побайтовое сравнение всех 11 файлов — PASS.
- `make build` — PASS.
- `git diff --check` — PASS.

## Незавершённое

- Следующая задача плана — Task 5: документация и project memory QuickTTY.

## Следующий шаг

1. Закоммитить подготовленный Task 4 и при переходе к Task 5 обновить README и AGENTS.md.

## Важный контекст

- Дизайн иконки утверждён пользователем; не менять композицию Q/TTY без нового согласования.
- Приложение не запускалось; выпуск, подпись, notarization и push не выполнялись.
