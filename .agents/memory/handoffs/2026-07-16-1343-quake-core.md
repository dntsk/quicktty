# Handoff: Quake presentation core

- **Дата:** 2026-07-16
- **Ветка:** `agent/quake-core`
- **Статус дерева:** изменения готовы к коммиту

## Выполнено

- Добавлены независимые normal/Quake оконные контейнеры и Quake geometry/animation/focus core.
- Добавлен `PresentationController` с единым content controller, rollback переходов и callback сохранения успешного режима.
- Добавлен интеграционный callback `toggleQuakeVisibility`; Carbon/config/coordinator не изменялись.

## Проверки

- `swift format lint <новые Swift-файлы>` — успешно.
- Targeted `PresentationStateMachineTests` — 7/7 успешно; test action также собрал application target.

## Незавершённое

- Core намеренно не подключён к `AppDelegate`/`WindowCoordinator` и global hotkey lane.

## Следующий шаг

1. В интеграционной ветке создать `PresentationController` с текущим workspace content controller и передать `toggleQuakeVisibility` hotkey controller.

## Важный контекст

- AppKit animation cancellation логическая: устаревшие completion игнорируются, последняя frame animation остаётся целевой.
- Автоскрытие использует внедряемые scheduler/suppression adapters; тесты не используют display, sleep или реальные приложения.
