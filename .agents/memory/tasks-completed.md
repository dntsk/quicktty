# Завершённые задачи

Новые записи добавляются сверху. Фиксируйте только завершённую и проверенную работу; подробности передавайте ссылками на ADR, integration contracts или handoff.

| Дата | Задача | Ключевые решения | Изменённые файлы |
|---|---|---|---|
| 2026-07-23 | Завершены dynamic tab titles и manual rename | Surface-targeted title callbacks строго копируют UTF-8, coalesce-ят automatic updates и безопасны при teardown/same-`PaneID` replacement. Effective title: persisted exact override > live active-pane title > Shell/Config fallback; automatic values не сохраняются. Inline rename поддерживает double-click/context menu, Enter/blur/Escape, exact-empty reset, raw whitespace/Unicode/emoji, focus restoration и Quake transient interaction. После исправления selected-but-inactive context rename открытых Critical/Important нет. Focused: 271 тест в 6 suites; финальный `make check`: 562 теста в 27 suites, 0 failures. Пользователь подтвердил manual smoke dynamic titles и manual rename на актуальной Debug-сборке; commit/push не сделаны. | `QuickTTY/Domain/`, `QuickTTY/Integration/GhosttyBridge/`, `QuickTTY/Presentation/TabBar/`, `QuickTTY/WindowCoordinator.swift`, `QuickTTYTests/`, `scripts/check-runtime-callbacks.sh`, `docs/plans/2026-07-23-dynamic-tab-titles*.md`, `docs/backlog.md`, `.agents/memory/` |
| 2026-07-23 | Реализован URL hover/open | Ghostty остаётся владельцем regex/OSC 8 detection, highlight, modifier и click semantics. QuickTTY строго копирует stable `open_url`, one-shot открывает schemes/standardized paths через injectable MainActor `NSWorkspace` client и применяет surface-local `mouse_shape` через cursor rects; pointer отображается pointing hand. `mouse_over_link`, preview и shortcut `open-url` отсутствуют, `copy-url`, selection и TUI mouse path не менялись. После integrated review focused `GhosttyBridgeTests` прошли 103 теста, callback contract и pin/symbol audit прошли; `make check` завершился успешно: 534 теста в 27 suites. Пользователь подтвердил manual smoke `Cmd+hover`/`Cmd+click` на актуальной Debug-сборке. | `QuickTTY/Integration/GhosttyBridge/`, `QuickTTYTests/Integration/GhosttyBridgeTests.swift`, `QuickTTYTests/Integration/GhosttyMouseInputTests.swift`, `scripts/check-runtime-callbacks.sh`, `docs/backlog.md`, `.agents/memory/`, `docs/plans/2026-07-23-url-hover-open*.md` |
| 2026-07-23 | Завершены configurable shortcuts QuickTTY | Все local actions принадлежат stable typed registry из 61 action ID и настраиваются строками `quicktty-shortcut`; `disabled`, sequential last-valid, removed-line reset, last-owner-wins, global precedence и Carbon rollback покрыты тестами. Ghostty `keybind` молча удаляется, effective config завершается `keybind = clear`, unmatched input сохраняет terminal/IME route. Typed terminal allowlist соблюдает performable fallback, focused-pane target и active-tab broadcast только для paste. После integrated review и исправления четырёх Important edge cases `make check` прошёл 525 тестов в 27 suites; pin Ghostty не менялся. | `QuickTTY/Input/`, `QuickTTY/Config/`, `QuickTTY/Integration/GhosttyBridge/`, `QuickTTY/AppDelegate.swift`, `QuickTTY/WindowCoordinator.swift`, `QuickTTYTests/`, `QuickTTY/Resources/`, `README.md`, `docs/backlog.md`, `.agents/memory/` |
| 2026-07-22 | Реализовано локальное восстановление ошибок terminal surface | Ошибка создания одной surface при startup/restore больше не завершает приложение и не откатывает соседние panes: model identity и split layout сохраняются, broadcast сбрасывается, а leaf показывает palette-aware placeholder `Terminal unavailable` с `Retry` и `Close Pane`. Retry создаёт fresh shell с тем же `PaneID` и saved CWD без запуска persisted custom command; Close Pane выполняет model-only collapse без Ghostty close или replacement shell. Generic missing surface, reentrant confirmation callbacks, inactive/background focus и tiny split viewport покрыты regressions. `make check` прошёл 473 теста в 26 suites; Ghostty pin и API не менялись. | `QuickTTY/Domain/WorkspaceStore.swift`, `QuickTTY/Presentation/Errors/`, `QuickTTY/Presentation/Splits/GhosttySplitTreeView.swift`, `QuickTTY/Presentation/WorkspaceViewController.swift`, `QuickTTY/WindowCoordinator.swift`, `QuickTTYTests/`, `docs/plans/2026-07-22-surface-failure-placeholder*.md` |
| 2026-07-22 | Опубликована подписанная QuickTTY alpha `0.1.0-alpha.2` | Public prerelease создан на GitHub из tag `v0.1.0-alpha.2` на проверенном commit `4b36744`. Arm64 DMG подписан Developer ID с hardened runtime, принят Apple (`fb215fc9-4de9-44bc-92b5-7506dd6f838b`), stapled и принят Gatekeeper как `Notarized Developer ID`. Public asset повторно скачан без авторизации и совпал по SHA-256: `fe3dac9780de7c23dab57caa34fdfb6c10f28323e6773c4e4423dbe7c3e8ca49`. Репозиторий переведён в public после проверки Git-истории на типовые секреты. | GitHub release `v0.1.0-alpha.2`, `.build/Release/QuickTTY-0.1.0-alpha.2-arm64.dmg`, `.agents/memory/handoffs/2026-07-22-1547-quicktty-public-alpha.md` |
| 2026-07-22 | Task 6: полностью завершено и проверено переименование в QuickTTY | Подтверждены clean start без чтения, переноса или удаления данных GhostTerm; identity `QuickTTY`/`com.dntsk.QuickTTY`, версия `0.1.0` build `2` и новая иконка. `make check` прошёл contracts, Debug build и 452 теста в 26 suites; Debug и unsigned Release bundles содержат ожидаемые metadata, arm64 executable, `ghostty` и `ThirdPartyNotices.txt`. Исторический GhostTerm alpha.1 остаётся неприкосновенным. | `QuickTTY/`, `QuickTTYTests/`, `project.yml`, `Makefile`, `scripts/`, `README.md`, `.agents/memory/` |
| 2026-07-22 | Task 3: release pipeline переименован в QuickTTY alpha.2 | Контракты release/notarize теперь фиксируют alpha.2, `QuickTTY.xcarchive`, QuickTTY DMG/stage, build `2`, bundle `com.dntsk.QuickTTY` и профиль по умолчанию `quicktty-notary`; старый `ghostterm-notary` остаётся допустимым явным env override. Cleanup allowlist не затрагивает исторические GhostTerm alpha.1 archive/DMG/evidence/stage. App-owned `QUICKTTY_*` заменили `GHOSTTERM_*`; Ghostty pin, ресурсные имена и `build-ghostty.sh` сохранены. `sh -n`, оба контракта и `make lint` прошли; release/notarize/signing не запускались. | `scripts/release-helpers.sh`, `scripts/build-release.sh`, `scripts/build-ghostty.sh`, `scripts/notarize-helpers.sh`, `scripts/notarize-dmg.sh`, `scripts/tests/` |
| 2026-07-22 | Выпущена подписанная alpha `0.1.0-alpha.1` | Release arm64 собран через Xcode, подписан Developer ID, принят Apple (`dead5cac-48a3-483f-b7c0-774c3dad56c9`), stapled и принят Gatekeeper как `Notarized Developer ID`. В bundle добавлены Ghostty terminfo/shell integration/themes, версия `0.1.0` build `1` и временная иконка GT. Пользователь установил DMG и подтвердил работу. Release/notarization scripts защищают пути, credentials и Make expansion. Два нестабильных AppKit-теста исправлены без изменения production; итоговый `make check` прошёл 446 тестов. DMG SHA-256: `0c0c7bc03d61bf8cfeb64708bb46a31c556bcea9897262cd1706c1120e6c7abf`. | `project.yml`, `Makefile`, `README.md`, `GhostTerm/Assets.xcassets/`, `GhostTerm/Resources/`, `scripts/`, `GhostTermTests/Integration/`, `docs/plans/2026-07-20-signed-alpha*.md` |
| 2026-07-22 | Закрыт тестовый пробел в проверке digest после notarization | Контракт notarize DMG фиксирует точную валидацию SHA-256 через `notarize_is_valid_sha256` после выделения финального digest и до его вывода; production-скрипт не изменялся. `sh -n`, `make notarize-contract` и `make lint` прошли. | `scripts/tests/notarize-dmg-test.sh`, `.agents/memory/tasks-completed.md` |
| 2026-07-16 | Завершена интеграция lifecycle split-pane | `WindowCoordinator` создаёт split surface с inherited cwd и shell-контекстом транзакционно, а process exit закрывает только leaf через `SplitCoordinator`. Focus surface меняет только model active pane; resize/equalize не пересоздают surfaces. SwiftUI representable имеет `.id(paneID)`: regression с двумя реальными Ghostty surfaces подтверждает, что после смены root в `NSHostingController` остаётся B, а не A. `make format`, `make lint` и `make build` прошли; isolated AppKit lifecycle runner остаётся нестабилен в headless environment. | `GhostTerm/Integration/GhosttyBridge/`, `GhostTerm/Presentation/Splits/`, `GhostTerm/Presentation/WorkspaceViewController.swift`, `GhostTerm/WindowCoordinator.swift`, `GhostTermTests/Presentation/` |
| 2026-07-16 | Исправлен echo watcher после атомарного изменения Quake height | `ConfigController.reload()` сравнивает точный `ConfigDocument` с `activeDocument` после чтения и не вызывает повторный apply для идентичных bytes. Изменённый внешний документ по-прежнему проходит parse/apply, включая прежнюю диагностику невалидной конфигурации. Focused Config tests: 16 passed; `make format` и `make lint` прошли. | `GhostTerm/Config/ConfigController.swift`, `GhostTermTests/Config/ConfigControllerTests.swift` |
| 2026-07-16 | Завершён Task 1: сохранение изменённой высоты Quake | `ConfigDocument` форматирует допустимую долю в locale-независимую процентную строку и меняет только bytes значения последнего ключа. `ConfigController` атомарно записывает source до транзакционного `apply`; ошибка reload откатывает active/effective config, но сохраняет уже записанный source. Focused Config tests: 15 passed; `make format` и `make lint` прошли. | `GhostTerm/Config/ConfigDocument.swift`, `GhostTerm/Config/ConfigController.swift`, `GhostTermTests/Config/ConfigDocumentTests.swift`, `GhostTermTests/Config/ConfigControllerTests.swift` |
| 2026-07-16 | Завершён Task 5 после spec PASS и quality APPROVED | Реализована чистая Domain-модель `StartupCommand`/pane/tab/workspace/store. Владение стабильными UUID валидируется глобально, а уникальные trim-имена — locale-stable. Перемещения вкладок атомарны и сохраняют порядок; при закрытии корректируется active tab, неактивные workspace сохраняются. Runtime broadcast сбрасывается только для текущего workspace и исключён из decode. `TerminalTab` атомарно поддерживает структурные инварианты. `StartupCommand` имеет явный tagged `Codable`; snapshot store использует обязательную валидируемую схему. `make check` прошёл 164 теста в 11 suites; Domain остаётся чистым, без AppKit/Ghostty | `GhostTerm/Domain/StartupCommand.swift`, `GhostTerm/Domain/TerminalPaneDescriptor.swift`, `GhostTerm/Domain/TerminalTab.swift`, `GhostTerm/Domain/Workspace.swift`, `GhostTerm/Domain/WorkspaceError.swift`, `GhostTerm/Domain/WorkspaceStore.swift`, `GhostTermTests/Domain/BroadcastSemanticsTests.swift`, `GhostTermTests/Domain/StartupCommandCodableTests.swift`, `GhostTermTests/Domain/TerminalTabTests.swift`, `GhostTermTests/Domain/WorkspaceSnapshotValidationTests.swift`, `GhostTermTests/Domain/WorkspaceStoreTests.swift` |
| 2026-07-16 | Завершён Task 4: типы идентичности и бинарное split-дерево после spec PASS и quality APPROVED | Введены отдельные UUID-обёртки `PaneID`, `TabID` и `WorkspaceID` с сохранением API `PaneID`. `SplitNode` реализует split/contains/leaves/remove-collapse/update ratio со стабильным порядком first→second. Ratio: конечные значения ограничиваются диапазоном 0.1...0.9, нечисловые нормализуются в 0.5; явный tagged `Codable` нормализует значения при encode/decode. `PresentationMode` перенесён в Domain. `make check` прошёл 99 тестов в 6 suites; Domain остаётся чистым и не зависит от AppKit/Ghostty | `GhostTerm/Domain/Identity.swift`, `GhostTerm/Domain/PresentationMode.swift`, `GhostTerm/Domain/SplitAxis.swift`, `GhostTerm/Domain/SplitNode.swift`, `GhostTermTests/Domain/SplitNodeTests.swift` |
| 2026-07-15 | Завершён Task 3: первая интерактивная terminal surface | Task 3A закрывает lifecycle, render, resize, focus, process exit и подтверждение закрытия; 3B — keyboard/IME с маршрутизацией исходного `NSEvent` отдельно для каждой surface; 3C — source-only mouse/scroll и clipboard/selection/unsafe paste/OSC52 с асинхронным exactly-once state machine и общей очередью подтверждений/закрытия окна. Реальные PTY-тесты покрывают key/unicode/mouse/copy/paste/OSC; resize проверен через C, закрытие окна — через `performClose`. Финальный `make check` прошёл 76 тестов в 5 suites; автоматизированные gates пройдены, spec PASS, quality APPROVED. `Vendor/ghostty` чист, сохранены точный pin, полный MIT notice и узкая first-party адаптация. Интерактивный системный IME, реальный `NSPasteboard` и видимые sheets вручную не проверены; terminfo/shell integration resources остаются будущей работой; broadcast из Task 10 не реализован | `GhostTerm/Integration/GhosttyBridge/`, `GhostTerm/WindowController.swift`, `GhostTerm/WindowCoordinator.swift`, `GhostTermTests/Integration/`, `scripts/`, `THIRD_PARTY_NOTICES.md`, `.agents/memory/` |
| 2026-07-15 | Завершён Task 3B после APPROVED по спецификации и качеству | Закреплённая upstream AppKit keyboard/IME-реализация узко адаптирована в first-party bridge; `Vendor/ghostty` не изменён и остаётся на точном commit `332b2aefc6e72d363aa93ab6ecfc86eeeeb5ed28`. Исходный `NSEvent` синхронно маршрутизируется через `PaneID` и слабую ссылку на bridge; перевод выполняется отдельно для каждой surface с поддержкой будущего broadcast, без повторного использования закодированных байтов. Реализованы key down/up/flags, key equivalents, `NSTextInputClient`, preedit и IME со scoped C lifetimes и очисткой при close. Включён полный MIT notice, документирована стратегия обновления. `make check` прошёл 39 тестов в 5 suites; детерминированный реальный PTY подтвердил точный ввод `a猫`, автоматизированно проверены protocol/C entrypoints. Интерактивный системный UI IME вручную не проверен; mouse/clipboard остаются в Task 3C | `GhostTerm/Integration/GhosttyBridge/GhosttyBridge.swift`, `GhostTerm/Integration/GhosttyBridge/GhosttyInput.swift`, `GhostTerm/Integration/GhosttyBridge/GhosttySurfaceView.swift`, `GhostTerm/Integration/GhosttyBridge/NSEvent+Ghostty.swift`, `GhostTermTests/Integration/GhosttyInputConversionTests.swift`, `GhostTermTests/Integration/GhosttyKeyboardInputTests.swift`, `THIRD_PARTY_NOTICES.md`, `.agents/memory/integration-contracts.md` |
| 2026-07-15 | Завершён Task 3A и исправлены три Important lifecycle finding | Metal surface встроена в единственное окно; lifecycle и process exit сохраняют `PaneID` и освобождают runtime/surface contexts без actor violation. Все C callbacks входят через top-level nonisolated функции; queued runtime action повторно проверяет active app и отбрасывается после shutdown. Live-process close сохраняет surface до AppKit confirmation sheet, cancel допускает повторный запрос, а false/confirmed close безопасно освобождают её; pending process exit имеет терминальный приоритет без второго actor hop. Display ID извлекается из `NSNumber`. `make check` прошёл 26 тестов в 4 suites; точный Debug bundle оставался жив 5 секунд и завершился штатно. Keyboard/IME и mouse/clipboard не входят в 3A и остаются в 3B/3C; terminfo/shell integration resources пока не bundled | `GhostTerm/Integration/GhosttyBridge/`, `GhostTerm/WindowController.swift`, `GhostTerm/WindowCoordinator.swift`, `GhostTermTests/Integration/`, `scripts/check-runtime-callbacks.sh`, `THIRD_PARTY_NOTICES.md`, `.agents/memory/integration-contracts.md` |
| 2026-07-14 | Исправлено последнее Important finding Task 2 | Production bootstrap с nil использует только built-in finalized libghostty defaults и не читает default Ghostty user config; explicit file сохраняет recursive includes; Task 7 позже передаст GhostTerm effective file. `make check` прошёл 17 тестов | `GhostTerm/Integration/GhosttyBridge/GhosttyConfiguration.swift`, `GhostTermTests/Integration/GhosttyBridgeTests.swift`, `.agents/memory/integration-contracts.md`, `.agents/memory/tasks-completed.md` |
| 2026-07-14 | Завершён Task 2 и исправлены Important findings code review | `ghostty_init` выполняется до `NSApplication`; independently C-retained callback context не владеет bridge и освобождается после `ghostty_app_free`; reload транзакционен по diagnostics и синхронно публикует `.configChanged`; реальный C union reload проверяет `soft`; hosted unit tests не создают production bridge/UI; cache stamp валидирует SHA-256 fat archive. `make check` прошёл 14 тестов | `GhostTerm/Integration/GhosttyBridge/`, `GhostTerm/ApplicationEnvironment.swift`, `GhostTerm/AppDelegate.swift`, `GhostTermTests/`, `scripts/build-ghostty.sh`, `README.md`, `.agents/memory/integration-contracts.md` |
| 2026-07-14 | Реализован Task 1: dependency/tooling для Ghostty | Ghostty v1.3.1 закреплён на `332b2aefc6e72d363aa93ab6ecfc86eeeeb5ed28`, Zig 0.15.2, static XCFramework линкуется без embed; `make check` прошёл, `GhosttyBridge` ещё не реализован | `.gitmodules`, `Vendor/ghostty`, `scripts/`, `Makefile`, `project.yml`, README и third-party/project memory |
| 2026-07-14 | Создан XcodeGen/AppKit skeleton | Swift 6/AppKit, macOS 15 arm64, application + unit test target, swift-format/Makefile; проверены xcodegen, lint и production typecheck, xcodebuild/test ожидают полного Xcode | Конфигурация проекта и исходники skeleton |
| 2026-07-14 | Создана project agent-инфраструктура | Навигационный `AGENTS.md`, русскоязычные rules/memory, безопасные проверочные скрипты | `AGENTS.md`, `.agents/` |
